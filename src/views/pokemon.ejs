<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pokemon.name %> - PokeDex</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .scrollable {
            max-height: 200px;
            overflow-y: auto;
        }
    </style>
</head>
<body class="bg-dark text-light container py-5">
    <div class="text-center">
        <img src="https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/<%= pokemon.id %>.png" alt="<%= pokemon.name %>" class="img-fluid mb-4" style="width: 150px; height: 150px;">

        <h1 class="mb-4"><%= pokemon.name %></h1>

        <p>Altura: <%= pokemon.height %> cm</p>
        <p>Peso: <%= pokemon.weight %> kg</p>

        <h2 class="mt-5">Habilidades:</h2>
        <div class="scrollable">
            <ul class="list-group bg-dark">
                <% pokemon.abilities.forEach(ability => { %>
                <li class="list-group-item bg-secondary text-light"><%= ability.ability.name %></li>
                <% }) %>
            </ul>
        </div>

        <h2 class="mt-5">Movimentos:</h2>
        <div class="scrollable">
            <ul class="list-group bg-dark">
                <% pokemon.moves.forEach(move => { %>
                <li class="list-group-item bg-secondary text-light"><%= move.move.name %></li>
                <% }) %>
            </ul>
        </div>

        <a href="/" class="btn btn-danger mt-5">Voltar</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
